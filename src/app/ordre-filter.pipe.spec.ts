import { OrdreFilterPipe } from './ordre-filter.pipe';

describe('OrdreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
