import { Ordreintervention } from './ordreintervention.model';

describe('Ordreintervention', () => {
  it('should create an instance', () => {
    expect(new Ordreintervention()).toBeTruthy();
  });
});
